# The GraphQL Workshop

## Requirements

- Node LTS
- npm >=7

## Setup

- `npm ci`

## Running the modules

- `cd src/step-{n}-{name}`
- check each module's README file to see which scripts are available

## Presenting

- `npm start`

